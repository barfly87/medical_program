<?php if (count($this->ta->principal_teacher_uid_arr) > 0): ?>
    <div id="tapteacher">
    <h2>Principal Teacher</h2><hr/>
        <?php
  		foreach($this->ta->principal_teacher_uid_arr as $uid) {
  			echo Compass::userInfoHtmlOnly($uid);
  		}
        ?>
     </div>
<?php endif;?>
